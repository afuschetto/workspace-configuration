###
### Experimental conversion from personal/bashrc_lnx
###

### Set general environment settings
# export EDITOR=emacs

### Set aliases

alias rm="rm -i"
alias grep="grep --color=always"
alias more="more -RS"
alias less="less -RS"
alias emacs="emacs -nw"
alias e="emacs"

### Set fzf (command-line fuzzy finder)
[[ -f ~/.fzf.zsh ]] && . ~/.fzf.zsh
